asyncapi: 3.0.0
info:
  title: Image Messaging API
  version: 1.0.29

channels:
  catalog.product.events:
    description: Product events (image service publishes here for ordering with product events)
    messages:
      ProductImagePromoted: {}
